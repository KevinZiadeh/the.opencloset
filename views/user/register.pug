extends ../layout

block content
  .register_window
    .register_box
      h1 Create an Acount
      form(method='POST', action='/users/register')
        .register_form
          //- label.register_label Name
          input.form-control.register_input(name='name', type='text', placeholder='name')
        .register_form
          //- label.register_label Email
          input.form-control.register_input(name='email', type='text', placeholder='email')
        .register_form
          //- label.register_label Username
          input.form-control.register_input(name='username', type='text', placeholder='username')
        .register_form
          //- label.register_label Password
          input.form-control.register_input(name='password', type='password', id='password', placeholder='password')
          .alert.alert-warning.pswrdRequirements(style="display: none")
            p Password should not contain a space at the end
            p Password should at least :
              ul
                li Contain one letter
                li Contain one number
                li Contain one symbol ~<>?!@#$%^&*()
                li Be 6 at least six letters
          .progress
            .progress-bar(role="progressbar", max='100', style='width: 0%', id='pswrdStrength')
        .register_form
          //- label.register_label Confirm Password
          input.form-control.register_input(name='password2', type='password', placeholder='confirm password')
        br
        input.register_button(id="submit-button" type='submit',value='Submit')
        br
        br
        //- a.btn.btn-primary(href='/users/login/facebook')
        //-   i.fab.fa-facebook-f 
        //-   span(style='margin-left: 5px') Continue via Facebook
